sudo: required

# ubuntu 20.04
dist: focal

language: minimal

services:
  - docker

before_script:
  - ls -al .
  - make cargo-fmt-check
  - ls -al .
  - make cargo-clippy

script:
  - |
    ls -al .
    make cargo-test
    ls -al .
    make assemble
