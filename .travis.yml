sudo: required

# ubuntu 20.04
dist: focal

language: minimal

services:
  - docker

before_script:
  - ls -al . && ls -al ./dist
  - make cargo-fmt-check
  - make cargo-clippy

script:
  - |
    ls -al . && ls -al ./dist
    make cargo-test &&
    make assemble
