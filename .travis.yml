sudo: required

language: python
python:
  - "3.6"

services:
  - docker

before_install:
  - ./scripts/setup-builder-docker.sh

before_script:
  - ./scripts/run_cacher.sh cargo-fmt.sh --all-check

script:
  - |
    ./scripts/run_tests.sh &&
    ./scripts/build.sh &&
    ./scripts/run_cacher.sh release-test.sh
